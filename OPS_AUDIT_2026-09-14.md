# OPS AUDIT — Peak Hour / Q-ALPHA — 2026-09-14 (US/Eastern)

**Auditor:** cloud agent (repo + GitHub + prior agent transcripts).  
**Laptop SoT:** `C:\Users\ajkle\Documents\Q-ALPHA` — **not readable from this VM.**  
**Default branch:** `origin/main` @ `9e991de` (2026-09-11 19:18 ET). **No main commits on 2026-09-14.**  
**Verdict:** Peak Hour **did run** on Monday, but **degraded**. Scheduler :15 ticks were on time for the hours we can see; SCAN Telegram lagged ~30 min; hour 8 aborted (`exit=-1`); trail flattened NX and a sync race fired a false `tsd_verify_missing` Telegram. Fixes for all three exist as **open PRs and are not live** (Aaron skipped laptop pull).

Evidence grades used below:

| Grade | Meaning |
|---|---|
| **AARON** | Operator-supplied timings / Telegram / log paraphrases given to 2026-09-14 agents. No raw laptop file was opened. |
| **CODE** | Behavior of `origin/main` (what the laptop would run if not pulled). |
| **GH** | GitHub PR / branch / merge state as of 2026-09-15 00:25 UTC. |
| **MISSING_LOCAL_LOGS** | Needs `candidates/logs/tsd_scheduler_2026-09-14.log`, `tsd_trail_2026-09-14.log`, `tws_sync_2026-09-14.log`, `tsd_scheduler_state.json`, `peak_hour_scans/php_scan_20260914_*.json`. All gitignored. |

---

## Scorecard

| Area | Score | One-line |
|---|---|---|
| 1. Scheduler duty (5 min tick, 1H DUE @ :15 hours 05–15) | **RAN_DEGRADED** | Duty cycle is correct in code/tasks. Hours 5–7, 9–10 DUE fired (AARON). Hour 8 aborted. Hours 11–15 unknown. |
| 2. Launch hours 05–15 | **RAN_DEGRADED** | See hour table. 8/11 hours evidenced; 1 abort; 5 hours missing. |
| 3. Trades (ATRC / NX / IRD / others) | **RAN_DEGRADED** | NX trail `base_break_down` ~09:30 ET (AARON) looks rule-named, not independently bar-checked. ATRC still open 2 sh at 09:30. IRD: no evidence. Sync false alarm on NX. |
| 4. Trail monitor | **RAN_OK** *(inferred)* | NX exit implies the loop was alive mid-morning. Heartbeats / action JSON: **MISSING_LOCAL_LOGS**. |
| 5. L2 / microstructure logger | **RAN_DEGRADED** | Research-only, not Peak Hour-scheduled. Laptop ran PR #4 path (HOOD/MSTR/TARS `--top 3` after Error 309). Expected 2152 / partial ARCA+NYSE+IEX. PR #6 (honest labels + depth-max 3) **not on main**. |
| 6. Options bridge `:8787` | **NOT_SCHEDULED** | BSF Phase 9A only. Peak Hour does not call it. Monday uptime: **UNKNOWN**. Last laptop smoke was Sat/Sun (hang, then event-loop crash, then fix on open PR #2). |
| 7. Deploy / PR #7 on default branch | **DID_NOT_RUN** *(fix)* | PR #7 **OPEN, not merged**. Live laptop (no pull) is Friday `main`. SCAN-lag, NX-verify, and L2-cap fixes are **GitHub-only**. |
| 8. Known issues (SCAN lag, hour 8, Error 162, NX race) | **RAN_DEGRADED** | SCAN lag + NX race **confirmed 09-14** and still unfixed on live. Hour 8 abort **confirmed that morning**. Error 162 is documented benign scanner teardown noise, not a 09-14 crash. |

**Overall day:** **RAN_DEGRADED** — not “didn’t run.” The desk was live; notification, one launch abort, and Cloud verify were the failures.

---

## Expected duty cycle (CODE)

Sole live entry authority is **QAlpha TSD Scheduler** → `scheduler.py --tick --live`.

| Job | When | Role |
|---|---|---|
| `QAlpha TSD Scheduler` | every **5 min**, 24h, Mon–Sun; weekend/holiday ticks no-op | At **:15 ET** hours **05–15**: print `DUE 1H launch` and run `run_1h_launch_scan(live=True)` |
| HTF universe | **04:30** and **12:00** ET (8-min due window) | Daily pass set |
| `QAlpha TSD Trail Monitor` | daily **04:00**, loop `--adaptive` to ~20:00 | Kill / BE / 4T keep-profit (clientId **95**) |
| `QAlpha Live TWS Sync` | prefer **07:00** ET, every **30 min** | Marks / Cloud (clientId **96**) |
| Weekly reports | Friday 17:00 only | N/A on Monday |
| Setup Watch / gap agent / Lab | **DISABLED** | Must stay off |

Launch window: `TICK_WINDOW_MIN = 8` — a :15 slot is only claimed if the tick lands within 8 minutes and `launch:YYYY-MM-DD:HH` is not yet in `tsd_scheduler_state.json`. `_mark_ran` happens **after** the launch returns. There is **no scheduler lock**. A 30-minute :15 scan plus a 5-minute task can overlap; a second :20 tick still sees the hour as DUE. That is the leading **CODE** explanation for hour-8 `exit=-1` (PowerShell `LASTEXITCODE=-1` = killed/terminated process). Not proven without the laptop log.

On **main**, SCAN Telegram is sent **after** enter + funnel + missed-ledger `mark_ran_up` + `push_dashboard_best_effort` (`tsd_1h_launch_scan.py`). That is why TG tracked full wall-clock.

---

## Hour-by-hour (2026-09-14 ET)

| Hour | DUE @ :15 | Tick exit | SCAN Telegram | `runtime_sec` / wall | Abort | Score | Evidence |
|---:|---|---|---|---|---|---|---|
| 05 | Fired (asserted) | unknown | ~28–37 min late | wall ~28–37 min; JSON `runtime_sec` band **1093–1629s** (all hours lumped) | no | **RAN_DEGRADED** | AARON |
| 06 | Fired (asserted) | unknown | same pattern | same | no | **RAN_DEGRADED** | AARON |
| 07 | Fired (asserted) | unknown | same pattern | same | no | **RAN_DEGRADED** | AARON |
| 08 | **DUE 08:15** | **exit=-1 @ 08:23:45** | **none** | ~8.75 min then dead | **YES** | **DID_NOT_RUN** *(scan)* | AARON — same historic abort class |
| 09 | **09:15:00** | implied 0 @ **09:45:45** | **~09:45** | funnel `php_scan_20260914_0915.json` ~**09:37:52**; then dual sync `missed_moves ~439–447`; wall **~30.8 min** | no | **RAN_DEGRADED** | AARON (best-documented hour) |
| 10 | **10:15:00** | implied 0 @ **10:46:19** | with Tick END | ~31 min “silent” after HTF cache; wall **~31.3 min** | no | **RAN_DEGRADED** | AARON |
| 11 | unknown | unknown | unknown | unknown | unknown | **UNKNOWN** | MISSING_LOCAL_LOGS (SCAN-lag prompt written ~11:04 ET) |
| 12 | unknown | unknown | unknown | unknown | unknown | **UNKNOWN** | MISSING_LOCAL_LOGS |
| 13 | unknown | unknown | unknown | unknown | unknown | **UNKNOWN** | MISSING_LOCAL_LOGS |
| 14 | unknown | unknown | unknown | unknown | unknown | **UNKNOWN** | MISSING_LOCAL_LOGS |
| 15 | unknown | unknown | unknown | unknown | unknown | **UNKNOWN** | MISSING_LOCAL_LOGS |

Hour-9/10 “silence” is **CODE-inferred**: serial Polygon 1H over ~147 HTF names + per-symbol TWS news connect (clientId 89, 8s timeout) + block-buffered stdout (PR #7 adds `PYTHONUNBUFFERED=1` + stage logs). Not a frozen scheduler.

Funnel counts (launches / take / entered / rejects) for every 09-14 hour: **MISSING_LOCAL_LOGS**.

---

## Trades

| Symbol | 09-14 action | Rule-correct? | Score |
|---|---|---|---|
| **NX** | Trail exit `base_break_down`, **7 sh @ ~20.63**, window **~09:30–09:37 ET**. Carry from week ending 09-11 (T1 already banked 09-11 09:37). | **Named correctly** (`tsd_trail_monitor` emits `base_break_down`). Bar/base proof **MISSING_LOCAL_LOGS**. Not a flatten bug in the 09-14 agents’ review. | **RAN_OK** *(exit)* / **RAN_DEGRADED** *(ops)* |
| **ATRC** | Still **OPEN 2 sh** at 09:30 TWS snapshot. Verify `ATRC: OPEN`. No 09-14 exit/entry in evidence. Carry from 09-09 (T1 banked 09-11). | Holding a keep-profit residual is intended. | **RAN_OK** *(as of 09:30)*; after 09:30 **UNKNOWN** |
| **IRD** | **Never mentioned** in repo, main, or any 09-14 agent prompt. | Cannot score. | **UNKNOWN** |
| Other new entries | None in 09-14 agent evidence. Funnel take/entered **MISSING**. | — | **UNKNOWN** |

### Sync failure (not a bad exit)

- **09:03** TWS sync: NX Cloud verify **OK** (AARON).
- **09:30** sync: start TWS `NX=7`, `ATRC=2`; logged `TSD upsert NX`; later verify `NX: (no row)` → `sync_errors=['tsd_verify_missing:NX']` → Telegram **`Peak Hour sync failed`** → process **exit=1**.
- **CODE:** verify uses the **start-of-run** open set. Trail closed NX and `prune_stale_tsd_positions` deleted the OPEN Cloud row while sync was still in watch_queue / missed_moves. Race, not a missing upsert.
- Fix is **PR #5** (`closed_during_sync`) — **OPEN, not on main**.

`tsd_mark_mismatch` was a **Friday 09-11** noise class (ATRC/NX). Not reported for 09-14.

---

## Trail monitor

| Expected | 09-14 |
|---|---|
| Task `QAlpha TSD Trail Monitor` @ 04:00 → `--loop --adaptive`, clientId 95, lock file | **CODE** |
| Heartbeat in `tsd_scheduler_state.json` (`trail_loop_heartbeat`, stale after 7 min) → tick skips backup | **MISSING_LOCAL_LOGS** |
| Actions / `trail_monitor_*.json` | **MISSING_LOCAL_LOGS** |
| Did it do work? | **Yes (AARON):** NX `base_break_down` mid-morning |

Score **RAN_OK** on the inference that only a live trail owner emits that exit. Confirm with `candidates/logs/tsd_trail_2026-09-14.log`.

---

## L2 / microstructure logger (research, clientId 72)

**Not on Peak Hour duty cycle.** Package is **absent from `main`**.

| Item | State |
|---|---|
| PR #4 logger | OPEN. First intended live day = Monday 09-14 if Aaron pulled the branch. |
| 09-14 live (AARON, to PR #6 agent) | Restarted `--symbols HOOD,MSTR,TARS --top 3` after **Error 309** (`--top 8` > 3 concurrent `reqMktDepth`). JSONL tagged `PARTIAL_IEX_SMART`. |
| Error **2152** | Expected: no NASDAQ/BATS/BEX depth. Entitled **ARCA / NYSE / IEX**. Not a crash. |
| PR #6 | Written **10:11–10:20 ET 09-14**. Caps depth at 3, labels `PARTIAL_ARCA_NYSE_IEX`, 2152/309 once, no Telegram. **OPEN, not pulled.** |
| Crash | None reported. |

Score **RAN_DEGRADED** (partial depth as designed; 309 until restart; honest-label fix not live).

---

## Options bridge `:8787` (research, clientId 71)

**NOT_SCHEDULED** for Peak Hour. Isolated package; Peak Hour does not import it. **Absent from `main`.**

Saturday–Sunday laptop smoke (PR #2): `/v1/health` OK; quote/chain hung; then `RuntimeError: There is no current event loop in thread 'options-bridge-ib'` after the timeout fix; patched in `fea1885`. **No 09-14 confirmation** the process was up or used.

---

## Deploy state (do not assume GitHub == laptop)

| PR | Title | State | On `main`? | Live if Aaron skipped pull? |
|---|---|---|---|---|
| **#7** | SCAN Telegram early + 1H cache/parallel + skip `mark_ran_up` on launch | **OPEN**, MERGEABLE | **No** | **No** |
| **#5** | `tsd_verify_missing` → `closed_during_sync` | **OPEN** | **No** | **No** |
| **#6** | L2 depth-max 3; expected 2152; no BATS/BEX | **OPEN** | **No** | **No** |
| **#4** | L2 logger v0 | **OPEN** | **No** | Only if branch was pulled locally (PR #6 prompt implies yes) |
| **#2** | Options bridge `:8787` | **OPEN** | **No** | Only if leftover weekend process |
| **#3 / #1** | LUCA brief / JANX flatten | DRAFT | No | N/A |

`main` is frozen at Friday 09-11 **3R shadow-paper DESIGN note**. Monday’s live Peak Hour code = that commit.

---

## Known issues — 09-14 status

| Issue | Status on 09-14 | Still live on next session if no pull? |
|---|---|---|
| SCAN TG ~30 min late (Telegram after serial Polygon + dual sync) | **Confirmed** hours 5–7, 9–10 | **Yes** until PR **#7** merged **and** laptop `git pull` |
| Hour 8 abort `exit=-1` | **Confirmed 08:15→08:23:45**, no SCAN | **Yes** — no code fix shipped; overlapping 5-min ticks + 30-min launch is the CODE hypothesis |
| Error 162 scanner noise | Documented benign teardown (`tws_scan_pipeline`); Peak Hour popularity scanners can emit it | Noise, not a 09-14 abort |
| NX `tsd_verify_missing` race (PR #5) | **Confirmed ~09:30** | **Yes** until PR **#5** merged **and** pulled |

---

## Failures ranked by severity

1. **Hour 8 launch abort (`exit=-1`, no SCAN)** — missed an entire hourly entry slot. Highest trading-desk impact. Cause unproven (7 overlapping-tick / Task Scheduler instance policy / TWS client 93). **MISSING_LOCAL_LOGS.**
2. **SCAN Telegram 28–37 min late** — desk-blind, not a missed DUE. Root cause on `main` is send-at-end-of-tick. **PR #7 fixes this.**
3. **False `Peak Hour sync failed` / `tsd_verify_missing:NX`** — dirty ops signal after a real (likely correct) trail exit. **PR #5 fixes this.**
4. **Hours 11–15 completely unauditable from GitHub** — cannot prove the afternoon ran.
5. **L2 Error 309 then `--top 3` workaround** — research-only; PR #6 cleans it. No Peak Hour impact.
6. **Options bridge leftover crash class** — not on the live path.

No critical one-line safety bug was patched in this audit (report-only). Overlapping scheduler ticks are a real ops hazard but not a one-liner.

---

## What Aaron should do next (before the next cash session)

1. **Merge and `git pull` PR #7** on `C:\Users\ajkle\Documents\Q-ALPHA` **before the next 05:15**. Without the pull, SCAN stays ~30 min late. Verify next :15: `DUE 1H launch` then `Telegram sent: Peak Hour SCAN hour=N (early; entered pending=True)` **before** `STAGE enter` / `push_dashboard`, plus flushed `stages:` line.
2. **Merge and pull PR #5** the same morning. Re-run `.\venv\Scripts\python.exe candidates\tws_intraday_sync.py --tsd-only` with TWS open; expect `sync_errors=none` / `closed_during_sync`, not Telegram `tsd_verify_missing`.
3. **On the laptop, grep the 09-14 logs** (this audit cannot):
   - `candidates\logs\tsd_scheduler_2026-09-14.log` — every `TICK START/END`, `DUE 1H`, `exit=`, hours **11–15**
   - `candidates\logs\tsd_trail_2026-09-14.log` — heartbeats, NX fill, ATRC after 09:30, any IRD
   - `candidates\logs\tws_sync_2026-09-14.log` — 09:03 vs 09:30 and later 30-min jobs
   - `candidates\tsd_scan_pipeline\results\tsd_scheduler_state.json` — `launch:2026-09-14:05` … `:15`
   - `peak_hour_scans\php_scan_20260914_*.json` — `runtime_sec`, take/entered
4. **Hour 8:** in Task Scheduler, set **QAlpha TSD Scheduler** to *Do not start a new instance* (or add a tick lock). A 30-min :15 scan + 5-min schedule is enough to kill/double-fire a slot.
5. **L2:** pull PR #6 only if the logger stays up; do not block Peak Hour on it.
6. **Do not** change structure_stop / BE / keep-profit / case ENTER from this audit. **Do not** re-enable Setup Watch / gap agent.

### Should PR #7 be merged/pulled before next session?

**Yes.** Merge on GitHub **and** pull on the laptop. GitHub-only does nothing — that is how 09-14 already failed. PR #5 should ride along. PR #6 is optional (research logger only).

---

## Sources

- `origin/main` `9e991de`; `candidates/tsd_scan_pipeline/scheduler.py`, `tsd_1h_launch_scan.py`, `register_tsd_tasks.ps1`, `DESIGN.md`, `PEAK_HOUR_PERFORMERS.md`
- Open PRs [#7](https://github.com/ajklepp/Q-ALPHA/pull/7), [#5](https://github.com/ajklepp/Q-ALPHA/pull/5), [#6](https://github.com/ajklepp/Q-ALPHA/pull/6), [#4](https://github.com/ajklepp/Q-ALPHA/pull/4), [#2](https://github.com/ajklepp/Q-ALPHA/pull/2)
- Prior agent transcripts: SCAN lag `bc-fde1418a…`, NX verify `bc-37e8867c…`, L2 `bc-0afb16e1…` / `bc-3ff64c7d…`, options `bc-dc5629c4…`
- Friday autopsy (context only, not 09-14): `candidates/tsd_scan_pipeline/results/LEARNING_AUTOPSY_20260911.md` (ATRC/NX still open EOW)
