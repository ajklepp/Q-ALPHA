# EXP-0025 — Sub-1H volatility clustering + fractal signatures

Question: after a Peak Hour-style 1H admit, do **pre-signal 5m**
vol-clustering / Hurst patterns separate continuation (hit-1R) from failure?

Hypothesis: clustering = “alive tape” (power-law world’s predictable piece);
sub-1H fractal persist vs chop = continuation vs failure tell.
